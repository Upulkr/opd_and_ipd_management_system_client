import LoginPage from "@/components/login/login-page";

/**
 * LogIn Page
 *
 * This component serves as the wrapper for the Login functionality.
 * It renders the full-page layout for user authentication.
 */
const LogIn = () => {
  return (
    <div className="w-full">
      <LoginPage />
    </div>
  );
};

export default LogIn;
